{
  "name": "aether-omni",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node -e \"require('fs').readdirSync('node_modules/.bin').forEach(f => require('fs').chmodSync('node_modules/.bin/' + f, 0o755))\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.51",
    "typescript": "^5.6.3",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.1"
  }
}
